{
  "name": "integration-tester-browser-extension",
  "version": "1.6.2",
  "description": "Sovendus integration tester chrome & firefox extensions",
  "type": "commonjs",
  "scripts": {
    "build": "npm run build-ts && npm run copy-files",
    "build-release": "npm run build-and-test --buildMode && npm run zip-builds",
    "build-ts": "npm run clean && tsc --build && rimraf build/chrome/tests",
    "soft-build": "tsc --build && rimraf build/chrome/tests && npm run copy-files",
    "debug-on-ff": "cd build/firefox && web-ext run",
    "clean": "rimraf build",
    "lint": "eslint src/**/*.ts --no-ignore --fix",
    "test": "jest --silent=false",
    "test-it": "jest --silent=false -t=isFlexibleIFrameExecutable",
    "build-and-test": "npm run build-test-zips && npm run test",
    "build-and-test-it": "npm run build-test-zips && npm run test-it",
    "build-test-zips": "npm run lint && npm run build && npm run zip-builds-for-tests",
    "copy-files": "npm run copy-other-files && npm run copy-to-ff && npm run copy-browser-specific-files",
    "copy-other-files": "cpy src/extension-pop-up/extension-pop-up.html build/chrome/extension-pop-up/ --flat && cpy src/npm/** build/chrome/npm/ && cpy src/sovendus.png build/chrome/ --flat",
    "copy-src": "cpy src/page-banner/*.ts build/chrome/src/page-banner/",
    "copy-to-ff": "cpy build/chrome/**/* build/firefox",
    "copy-browser-specific-files": "cpy src/chrome/* build/chrome && cpy src/firefox/* build/firefox",
    "zip-builds": "zip-build build/chrome release_zips -t 'chrome-test-sovendus-integration_%VERSION%_%TIMESTAMP%.%EXT%' && zip-build build/firefox release_zips -t 'firefox-test-sovendus-integration_%VERSION%_%TIMESTAMP%.%EXT%'",
    "zip-builds-for-tests": "rimraf test_zips && zip-build build/chrome test_zips -t 'chrome-test-sovendus-integration_TESTING.crx' && zip-build build/firefox test_zips -t 'firefox-test-sovendus-integration_TESTING.xpi'"
  },
  "author": "Marcus Brandstaetter - Sovendus GmbH",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@floating-ui/dom": "^1.6.10",
    "@types/chrome": "^0.0.270",
    "@types/crypto-js": "^4.2.2",
    "@types/firefox-webext-browser": "^120.0.4",
    "@types/jest": "^29.5.12",
    "@types/node": "22.5.2",
    "@types/selenium-webdriver": "^4.1.26",
    "cpy-cli": "^5.0.0",
    "crypto-js": "^4.2.0",
    "dotenv": "^16.4.5",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.30.0",
    "eslint-plugin-prettier": "^5.2.1",
    "eslint-plugin-simple-import-sort": "^12.1.1",
    "eslint-plugin-unused-imports": "^4.1.3",
    "jest": "^29.7.0",
    "prettier": "^3.3.3",
    "prettier-eslint": "^16.3.0",
    "rimraf": "^6.0.1",
    "run": "^1.5.0",
    "selenium-webdriver": "^4.24.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4",
    "typescript-eslint": "^8.4.0",
    "web-ext": "^8.2.0",
    "zip-build": "^1.8.0"
  }
}